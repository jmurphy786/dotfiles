# Only search specific project directories, not entire home
TS_SEARCH_PATHS=(~/Documents/Github  ~/notes)

# Or if you want to search multiple levels deep:
# TS_SEARCH_PATHS=(~/projects:2 ~/work:2 ~/notes:1)

# Optional: Set max depth (default is 1)
TS_MAX_DEPTH=1
